export const createRule = {
    "name": "required",
    "action": "required",
    "subject": "required",
    "conditions": "required"
};

export const updateRule = {
    "name": "string",
    "action":  "string",
    "subject":  "string",
    "conditions": "string"
};